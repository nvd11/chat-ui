# Service: Exposes the deployment pods internally within the cluster
apiVersion: v1
kind: Service
metadata:
  name: chat-ui-iap-service
spec:
  type: ClusterIP
  selector:
    app: chat-ui-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
