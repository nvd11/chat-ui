# Service: Exposes the deployment pods internally, WITHOUT IAP.
# This service is intended for in-cluster communication.
apiVersion: v1
kind: Service
metadata:
  name: chat-ui-frontend-service
spec:
  type: ClusterIP
  selector:
    app: chat-ui-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
